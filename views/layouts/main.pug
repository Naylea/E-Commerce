doctype html
head
  meta(charset='UTF-8')
  link(rel='icon' href='/public/logo.ico')
  script(src='https://kit.fontawesome.com/0f8125221d.js' crossorigin='anonymous')
  title  Deli Healthy
  link(rel="stylesheet", href="https://bootswatch.com/4/minty/bootstrap.min.css")
body
    //- if (!user)
    //-     a(href="/signin") Signin
    //-     a(href="/signup") Signup
    //- else
    //-     a(href="/logout") Logout
    //-     a(href="/profile") Profile

    nav(class="navbar navbar-expand-lg navbar-dark bg-primary")
        a(class="navbar-brand" href="/Bienvenido") Deli Healthy <i class="fas fa-utensils"></i>
        button(class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation")
            span(class="navbar-toggler-icon")
        div(class="collapse navbar-collapse" id="navbarNav")
            ul(class="navbar-nav ml-auto" )
                if(user)
                    li.nav-item.active
                        a.nav-link(href='/profile' ) <i class="fas fa-user"></i> Perfil
                    li.nav-item
                        a.nav-link(href='logout') <i class="fas fa-sign-out-alt"></i> Salir
                else
                    li.nav-item.active
                        a.nav-link(href='/signin') <i class="fas fa-user-circle"></i> Iniciar Sesión 
                    li.nav-item
                        a.nav-link(href='signup') <i class="fas fa-user-plus"></i> Registrarse
br
br
footer

    div(class='container' align="center")
        if(mensajedeinicio)
            div(class="card text-white bg-danger mb-3" style="max-width: 20rem")
                div(class="card-header") Error
                div(class="card-body")
                    h4(class="card-title") #{mensajedeinicio}
                    p(class="card-text") 

        if(mensajederegistro)
            div(class="card text-white bg-danger mb-3" style="max-width: 20rem")
                div(class="card-header") Error
                div(class="card-body")
                    h4(class="card-title") #{mensajederegistro}
                    p(class="card-text") 
